# Version Signal

Returns the current stable version and publication date of any npm or PyPI package, sourced from the canonical registry. A free, no-signup lookup tool with a browser UI and a machine-readable API, hosted on a Cloudflare Worker.

- **Provider:** https://version-signal.inboxtzdjqv.workers.dev/
- **Documentation:** https://version-signal.inboxtzdjqv.workers.dev/
- **Base URL:** https://version-signal.inboxtzdjqv.workers.dev
- **Tags:** developer tools, package registry, npm, pypi, versions, software supply chain, dependency management, open-source metadata
- **Public API:** yes
- **Agent-native (MCP / llms.txt / skills):** yes
- **Confidence:** 88/100

## APIs

- **Version Signal API** — Free, no-auth REST API returning the current stable version and publication date of npm and PyPI packages from the canonical registry. (`https://version-signal.inboxtzdjqv.workers.dev`)

> Homepage was directly fetched and is live; it doubles as the documentation page (free, no signup). /openapi.json, /llms.txt, and /.well-known/apis.json are confirmed to exist via well-known detection and the submitter's apis.json, but their raw bodies were not retrievable in-session, so exact endpoint paths/schemas of the free API are unverified. No MCP server and no published agent skills were found. Agent-native monetization is handled via a SEPARATE x402-payable sibling API on a different subdomain — Dependency Truth / 'Agent Evidence Utilities' (v1.1.0) at https://dependency-truth.inboxtzdjqv.workers.dev/openapi.json, with POST routes /v1/dependency-truth ($0.005), /v1/package-risk ($0.006), /v1/repo-pulse ($0.004), and /v1/citation-evidence ($0.004), each returning 402 until paid via x402 (x402scan server id 33575deb-16a7-49fc-b118-f49330f34632). That sibling should likely be cataloged as its own entry.

_Generated by apis.io from a community submission._